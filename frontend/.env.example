# Frontend environment variables (only used in production build)
# In development, Vite proxies /api to localhost:3001 automatically.
# For production (Railway), set this to your deployed backend URL.
VITE_API_URL=https://your-railway-backend.railway.app
